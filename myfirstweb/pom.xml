<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>net.slipp</groupId>
	<artifactId>myfirstweb</artifactId>
	<version>1.0</version>
	<packaging>war</packaging>
	
	<dependencies>
	
		<!-- 
			groupId, artifactId, version을 읽어
			Library저장소에서 알맞는 라이브러리를 내 로컬로 다운로드를 자동으로 한다.
			내 로컬은 유저의 repository -> 각 lib이름 ->lib이름 ->버전별로관리
			
			내가 추가한 Lib가 아닌데 추가되어있는 Library가 존재할 수 있다.
			이는 내가 추가한 Lib가 설정하지 Lib에 의존성을 가지고 있기 때문이다.
		 -->
		<!-- junit Library -->
		<dependency>
		    <groupId>junit</groupId>
		    <artifactId>junit</artifactId>
		    <version>4.11</version>
		</dependency>
		
		<!-- 
		com  -> google -> code -> gson순으로 디렉토리 생성
		 -->
		<dependency>
		    <groupId>com.google.code.gson</groupId>
		    <artifactId>gson</artifactId>
		    <version>2.3</version>
		</dependency>
		
	</dependencies>
	
</project>